// components/site/navbar.tsx
import { Button } from "@/components/ui/button";

export function Navbar() {
  return (
    <nav>
      {/* Other navbar elements */}
      <Button asChild className="cursor-pointer bg-blue-600 hover:bg-blue-700">
        {/* Button content */}
      </Button>
      {/* Other navbar elements */}
    </nav>
  );
}

// app/services/[slug]/page.tsx
import { Button } from "@/components/ui/button";

export default function ServicePage() {
  return (
    <div>
      {/* Other page elements */}
      <Button asChild variant="outline" className="cursor-pointer bg-transparent">
        {/* Button content */}
      </Button>
      <Button asChild className="cursor-pointer bg-blue-600 hover:bg-blue-700">
        {/* Button content */}
      </Button>
      {/* Other page elements */}
    </div>
  );
}

// app/blog/page.tsx
import { Button } from "@/components/ui/button";

export default function BlogPage() {
  return (
    <div>
      {/* Other page elements */}
      <Button asChild variant="outline" className="cursor-pointer bg-transparent">
        {/* Button content */}
      </Button>
      {/* Other page elements */}
    </div>
  );
}

// app/blog/[slug]/page.tsx
import { Button } from "@/components/ui/button";

export default function BlogPostPage() {
  return (
    <div>
      {/* Other page elements */}
      <Button asChild variant="outline" className="mb-6 bg-transparent cursor-pointer">
        {/* Button content */}
      </Button>
      {/* Other page elements */}
    </div>
  );
}

// app/careers/page.tsx
import { Button } from "@/components/ui/button";

export default function CareersPage() {
  return (
    <div>
      {/* Other page elements */}
      <Button asChild variant="outline" className="cursor-pointer bg-transparent">
        {/* Button content */}
      </Button>
      <Button asChild className="cursor-pointer bg-blue-600 hover:bg-blue-700">
        {/* Button content */}
      </Button>
      {/* Other page elements */}
    </div>
  );
}
